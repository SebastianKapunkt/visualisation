class Result {
  constructor(id, time, x, y) {
    this.id = id;
    this.time = time;
    this.x = x;
    this.y = y;
    console.log("id: " + id);
    console.log("time: " + time);
    console.log("x: " + x + " y: " + y);
  }
}
